# Server-side only — never commit real values or expose to the browser.
# Local: copy to .env.local
# Production: set in your host dashboard (Render, Railway, etc.)

# Required for public deployment — raises GitHub limit from 60/hr to 5000/hr
# Create at: https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=
